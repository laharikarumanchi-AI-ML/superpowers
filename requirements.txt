# Requirements for Hugging Face Spaces deployment of the Streamlit demo.
# HF reads this file at build time to install dependencies into the Space.
# Local development uses pyproject.toml instead — see docs/HF_DEPLOY.md.

streamlit>=1.31.0
jupyter_client>=8.6.0
ipykernel>=6.29.0
requests>=2.31.0
pandas>=2.2.0
matplotlib>=3.8.0

# Note: we deliberately do NOT install the agent package via pip here.
# HF Spaces mounts requirements.txt at /tmp at install time, but the
# repo source isn't available yet at that step — so any `-e .` or
# `-e /app` reference fails ("no pyproject.toml found"). Instead,
# demo/app.py adds the repo root to sys.path explicitly, which is
# portable and works in both local and HF environments.
